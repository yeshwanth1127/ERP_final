# Session secret (change in production)
SECRET_KEY=your-secret-key

# n8n webhook base URL. Use full path including /webhook, or omit it (added automatically).
# Examples:
#   With /webhook:  https://n8n.exora.solutions/webhook
#   Without:        https://n8n.exora.solutions
#   Local:          http://localhost:5678/webhook
N8N_BASE_URL=https://n8n.exora.solutions/webhook

# Optional: webhook to execute SQL (e.g. n8n runs the query). If not set, "Run SQL" returns 501.
# N8N_EXECUTE_SQL_WEBHOOK=https://n8n.exora.solutions/webhook/execute-sql

# Optional: webhook to clear the vector store (e.g. n8n runs TRUNCATE rag_schema_docs). If not set, "Clear vector store" returns 501.
# N8N_CLEAR_VECTOR_WEBHOOK=https://n8n.exora.solutions/webhook/clear-vector
